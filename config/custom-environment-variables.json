{
    "port": "PORT",
    "host": "HOST",
    "env": "NODE_ENV",
    "aws": {
      "accessKeyId": "AWS_ACCESS_KEY_ID",
      "secretAccessKey": "AWS_SECRET_ACCESS_KEY",
      "region": "AWS_REGION",
      "s3": {
        "bucket": "AWS_S3_BUCKET"
      }
  }
}
